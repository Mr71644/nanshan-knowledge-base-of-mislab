# 忘记密码 — 邮件验证码重置

## 业务流程

```
前端登录页 → 点击"忘记密码" → 输入邮箱
  → 调接口1 发送验证码
  → 用户输入验证码 + 新密码 + 确认密码
  → 调接口2 重置密码
  → 返回登录页，用新密码登录
```

---

## 接口1：发送重置密码验证码

### 基本信息

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /user/password/reset-code` |
| **请求方式** | `POST` |
| **认证** | 无需登录 |
| **Content-Type** | `application/json` |

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|:--:|------|
| `email` | String | 是 | 注册时使用的邮箱，格式必须合法 |

### 请求示例

```json
{
  "email": "user@example.com"
}
```

### 响应示例

**成功：**

```json
{
  "code": 200,
  "message": "success",
  "data": "验证码已发送到您的邮箱，请查收（有效期5分钟）"
}
```

**失败：**

| code | message | 说明 |
|:----:|---------|------|
| 400 | `"该邮箱未注册"` | 邮箱在系统中不存在 |
| 403 | `"该用户已被禁用，无法重置密码"` | 该邮箱对应的用户已被禁用 |
| 500 | `"验证码发送失败，请稍后重试"` | 邮件服务异常 |

### 验证码规则

- 6 位数字（如 `384729`）
- 有效期 **5 分钟**
- 同一邮箱重复调用会覆盖之前的验证码

---

## 接口2：重置密码

### 基本信息

| 项目 | 内容 |
|------|------|
| **接口路径** | `POST /user/password/reset` |
| **请求方式** | `POST` |
| **认证** | 无需登录 |
| **Content-Type** | `application/json` |

### 请求参数

| 参数名 | 类型 | 必填 | 校验规则 |
|--------|------|:--:|------|
| `email` | String | 是 | 邮箱格式合法 |
| `code` | String | 是 | 6 位数字 |
| `newPassword` | String | 是 | 长度 6-20 位 |
| `confirmPassword` | String | 是 | 必须与 `newPassword` 一致 |

### 请求示例

```json
{
  "email": "user@example.com",
  "code": "384729",
  "newPassword": "abc123456",
  "confirmPassword": "abc123456"
}
```

### 响应示例

**成功：**

```json
{
  "code": 200,
  "message": "success",
  "data": "密码重置成功，请使用新密码登录"
}
```

**失败：**

| code | message | 说明 |
|:----:|---------|------|
| 500 | `"该邮箱未注册"` | 邮箱在系统中不存在 |
| 500 | `"该用户已被禁用，无法重置密码"` | 用户已被禁用 |
| 500 | `"两次输入的密码不一致"` | newPassword ≠ confirmPassword |
| 500 | `"验证码错误或已过期"` | 验证码不正确或超过5分钟 |
| 500 | `"密码加密失败"` / `"密码重置失败"` | 服务端异常 |

### 注意事项

- 验证码**一次性使用**，重置成功后立即失效
- 新密码会 **SHA-256 加密**后存入数据库
- 操作为**事务性**，失败时密码不会被修改

---

## 前端交互建议

```
┌─────────────────────────────────┐
│         忘记密码页面              │
│                                 │
│  邮箱: [________________]       │
│         [发送验证码]  ← 接口1    │
│  验证码: [______]               │
│  新密码: [________________]     │
│  确认密码: [________________]   │
│         [重置密码]    ← 接口2    │
│                                 │
└─────────────────────────────────┘
```

### 状态处理

| 操作 | 建议 |
|------|------|
| 点击"发送验证码" | 按钮置灰，显示倒计时 60 秒（防止重复点击） |
| 发送成功 | 提示"验证码已发送"，输入框自动聚焦 |
| 发送失败 | 弹窗提示具体错误信息，按钮恢复可点击 |
| 点击"重置密码" | 前端校验：两次密码一致、密码长度、验证码非空 |
| 重置成功 | 提示"密码重置成功"，2 秒后跳转登录页 |
| 验证码过期 | 提示"验证码已过期，请重新获取"，清除验证码输入框 |

### 防重复发送

```javascript
let countdown = 0;
const timer = setInterval(() => {
  if (countdown <= 0) {
    clearInterval(timer);
    btn.disabled = false;
    btn.textContent = '发送验证码';
  } else {
    btn.textContent = `${countdown}s 后重发`;
    countdown--;
  }
}, 1000);
```
