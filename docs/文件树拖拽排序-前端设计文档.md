# 文件树拖拽排序功能 — 前端设计文档

## 一、需求背景

在左侧文件树中，通过鼠标拖拽方式实现：

1. **同级排序**：同一父文件夹内拖拽调整顺序
2. **跨层移动**：将文件/文件夹从一个文件夹拖到另一个文件夹中

## 二、技术方案

### 2.1 核心思路

侧边栏从 Ant Design `Menu` 组件替换为 `Tree` 组件，利用其内置 `draggable` + `allowDrop` + `onDrop` 实现拖拽。

### 2.2 需要修改的文件

| 文件路径 | 变更内容 |
|---------|---------|
| `src/apis/fileList.js` | 新增 `sortTreeItems` + `moveTreeItem` API 函数 |
| `src/views/Home/index.jsx` | `Menu` → `Tree` 重构 + 拖拽逻辑 |
| `src/views/Home/index.module.less` | 样式从 Menu 选择器迁移到 Tree 选择器 |

## 三、API 函数

### 3.1 同层排序（已实现）

```javascript
const sortTreeItems = (parentFolderId, orderedIds) => {
    return request({
        url: '/home/sort',
        method: 'PUT',
        data: { parentFolderId, orderedIds }
    })
}
```

### 3.2 跨层移动（新增）

```javascript
const moveTreeItem = (itemId, itemStatus, targetFolderId) => {
    return request({
        url: '/home/move',
        method: 'PUT',
        data: { itemId, itemStatus, targetFolderId }
    })
}
```

参数说明：
- `itemId`：被移动的文件/文件夹 ID
- `itemStatus`：类型（1=文档 2=文件夹 3=Excel 4=文件）
- `targetFolderId`：目标文件夹 ID，`null` 表示移到根目录

```javascript
export {
    getFileList,
    togglePin,
    sortTreeItems,
    moveTreeItems
}
```

## 四、Menu → Tree 重构

（已完成，此处略。详见 git 历史。）

## 五、拖拽逻辑

### 5.1 allowDrop — 控制可放置区域

```javascript
const handleAllowDrop = ({ dragNode, dropNode, dropPosition }) => {
    const dragItem = dragNode.rawData
    const dropItem = dropNode.rawData

    // 规则 1：放在节点上（dropPosition === 0）
    //   仅当目标是文件夹(status=2)时允许（移入该文件夹）
    if (dropPosition === 0) {
        return dropItem.status === 2
    }

    // 规则 2：放在间隙（gap）中 → 始终允许
    //   包括同层排序和跨层移动到目标同级

    // 规则 3：防循环 — 拖拽文件夹时，禁止拖入自身子树
    if (dragItem.status === 2 && isDescendant(folderTree, dragItem.id, dropItem)) {
        return false
    }

    return true
}
```

### 5.2 防循环引用检查

```javascript
// 检查 target 是否是 folderId 的后代（或自身）
const isDescendant = (tree, folderId, target) => {
    // 找到 folderId 对应的节点
    const findNode = (data, id) => {
        for (const item of data) {
            if (item.id === id) return item
            if (item.children) {
                const found = findNode(item.children, id)
                if (found) return found
            }
        }
        return null
    }

    // 检查 target 是否在 folderId 的子树中
    const isInSubtree = (node, targetItem) => {
        if (!node) return false
        if (node.id === targetItem.id && node.status === targetItem.status) return true
        if (node.children) {
            return node.children.some(child => isInSubtree(child, targetItem))
        }
        return false
    }

    const folderNode = findNode(tree, folderId)
    return isInSubtree(folderNode, target)
}
```

### 5.3 处理拖放（onDrop）

根据拖放类型分两种处理：

```javascript
const handleDrop = async (info) => {
    const dragNode = info.dragNode
    const dropNode = info.node
    const dragItem = dragNode.rawData
    const dropItem = dropNode.rawData

    const dragFolderId = dragItem.folderId ?? null
    const dropFolderId = dropItem.folderId ?? null

    // 乐观更新：先移动本地树数据
    const newTreeData = reorderTreeData(folderTree, dragNode, dropNode, info.dropToGap, info.dropPosition)
    SetFolderTree(newTreeData)

    if (!info.dropToGap && dropItem.status === 2) {
        // 情况 A：拖入文件夹（dropPosition === 0，目标是文件夹）
        const targetFolderId = dropItem.id
        try {
            await moveTreeItem(dragItem.id, dragItem.status, targetFolderId)
        } catch (e) {
            error({ content: '移动失败，请重试' })
            getTree()
        }
    } else if (dragFolderId === dropFolderId) {
        // 情况 B：同层排序
        const siblings = findSiblingsByParentId(newTreeData, dropFolderId)
        const orderedIds = siblings.map(item => ({ id: item.id, status: item.status }))
        try {
            await sortTreeItems(dropFolderId, orderedIds)
        } catch (e) {
            error({ content: '排序保存失败，请重试' })
            getTree()
        }
    } else {
        // 情况 C：跨层 gap drop（拖到另一层级的间隙中）
        const targetFolderId = dropFolderId
        try {
            await moveTreeItem(dragItem.id, dragItem.status, targetFolderId)
        } catch (e) {
            error({ content: '移动失败，请重试' })
            getTree()
        }
    }
}
```

### 5.4 reorderTreeData

```javascript
const reorderTreeData = (treeData, dragNode, dropNode, dropToGap, dropPosition) => {
    const newTreeData = JSON.parse(JSON.stringify(treeData))
    const dragKey = dragNode.key
    const dropKey = dropNode.key

    const loop = (data, key, callback) => {
        for (let i = 0; i < data.length; i++) {
            if (getMenuKeyByItem(data[i]) === key) {
                return callback(data[i], i, data)
            }
            if (data[i].children) {
                loop(data[i].children, key, callback)
            }
        }
    }

    // 从原位置移除拖拽节点
    let dragObj
    loop(newTreeData, dragKey, (item, index, arr) => {
        arr.splice(index, 1)
        dragObj = item
    })

    if (!dropToGap) {
        // 拖入目标节点内部（成为子节点）
        loop(newTreeData, dropKey, (item) => {
            item.children = item.children || []
            item.children.unshift(dragObj)
        })
    } else {
        // 拖入目标节点之间的间隙
        const dropPosArr = dropNode.pos.split('-')
        const dropIndex = Number(dropPosArr[dropPosArr.length - 1])
        const relativePosition = dropPosition - dropIndex

        let ar, i
        loop(newTreeData, dropKey, (_item, index, arr) => {
            ar = arr
            i = index
        })

        if (relativePosition === -1) {
            ar.splice(i, 0, dragObj)
        } else {
            ar.splice(i + 1, 0, dragObj)
        }
    }

    return newTreeData
}
```

### 5.5 辅助函数

```javascript
// 根据 parentFolderId 找到同级兄弟节点数组
const findSiblingsByParentId = (treeData, parentFolderId) => {
    if (parentFolderId === null) return treeData
    for (const item of treeData) {
        if (item.id === parentFolderId) return item.children || []
        if (item.children) {
            const found = findSiblingsByParentId(item.children, parentFolderId)
            if (found.length > 0) return found
        }
    }
    return []
}
```

## 六、测试验证清单

### 同层排序

- [ ] 同层级拖拽排序正常
- [ ] 拖拽后刷新页面，顺序保持
- [ ] 网络失败时自动回滚 + 显示错误提示

### 跨层移动

- [ ] 拖拽文件到文件夹上 → 文件移入该文件夹
- [ ] 拖拽文件夹到另一个文件夹上 → 整棵子树移入
- [ ] 拖拽到根目录层级 → 移到最外层
- [ ] 拖拽文件夹到自身子文件夹 → 被阻止（防循环）
- [ ] 拖拽文件到非文件夹（文档/Excel）上 → 被阻止
- [ ] 刷新后移动结果保持

### 视觉与导航

- [ ] 展开/折叠动画平滑
- [ ] 各类型图标正确
- [ ] 点击导航正常（文件夹、文档、Excel、文件）
- [ ] 面包屑同步更新

### 边界情况

- [ ] 只有 1 个子项时拖拽不崩溃
- [ ] 3 层以上嵌套正常工作
- [ ] 快速连续拖拽无异常
