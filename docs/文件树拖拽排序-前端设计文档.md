# 文件树拖拽排序功能 — 前端设计文档

## 一、需求背景

在左侧文件树中，通过鼠标拖拽方式调整同层级文件/文件夹的显示顺序。

- 本期只实现**同级排序**（同一父文件夹内拖拽调整顺序）
- 跨文件夹移动作为后续功能，本期不实现

## 二、技术方案

### 2.1 核心思路

当前侧边栏使用 Ant Design `Menu` 组件，`Menu` 是导航组件，不支持拖拽。将其替换为 Ant Design `Tree` 组件：

- `Tree` 内置 `draggable` + `allowDrop` + `onDrop`，原生支持拖拽
- 项目其他视图（Administrator、Excel）已在用 `Tree`
- 无需安装额外 npm 依赖

### 2.2 需要修改的文件

| 文件路径 | 变更内容 |
|---------|---------|
| `src/apis/fileList.js` | 新增 `sortTreeItems` API 函数 |
| `src/views/Home/index.jsx` | `Menu` → `Tree` 重构 + 拖拽排序逻辑 |
| `src/views/Home/index.module.less` | 样式从 Menu 选择器迁移到 Tree 选择器 |

无需新增文件，无需安装新的 npm 包。

## 三、新增 API 函数

在 `src/apis/fileList.js` 中新增：

```javascript
const sortTreeItems = (parentFolderId, orderedIds) => {
    return request({
        url: '/home/sort',
        method: 'PUT',
        data: {
            parentFolderId,
            orderedIds
        }
    })
}

export {
    getFileList,
    togglePin,
    sortTreeItems
}
```

对接的后端接口：`PUT /home/sort`（详见后端设计文档）。

## 四、Menu → Tree 重构

### 4.1 导入变更

`src/views/Home/index.jsx` 第 3 行：

```javascript
// 之前：
import { Layout, Menu, theme, ... } from 'antd';

// 之后：
import { Layout, Tree, theme, ... } from 'antd';
```

### 4.2 数据转换函数

将 `transformToMenuItems`（约第 232-280 行）替换为 `transformToTreeData`：

```javascript
const transformToTreeData = (data) => {
    return data.map(item => {
        const hasChildren = item.children && item.children.length > 0
        return {
            key: getMenuKeyByItem(item),
            icon: getIconByStatus(item.status),
            isLeaf: !hasChildren,
            children: hasChildren ? transformToTreeData(item.children) : [],
            rawData: item
        }
    })
}

const getIconByStatus = (status) => {
    if (status === 1) return <EditOutlined />
    if (status === 2) return <FolderOutlined />
    if (status === 3) return <TableOutlined />
    if (status === 4) return <FileOutlined />
}
```

与原函数的区别：
- 不再在每个节点中内嵌展开箭头（由 `switcherIcon` 统一处理）
- 不再内嵌 `Tooltip`（由 `titleRender` 统一处理）
- 新增 `rawData` 字段，保存原始数据供拖拽逻辑使用

### 4.3 节点渲染（titleRender）

```javascript
const renderTitle = (nodeData) => {
    const item = nodeData.rawData
    return (
        <Tooltip title={item.name}>
            <span>{item.name}</span>
        </Tooltip>
    )
}
```

### 4.4 展开箭头（switcherIcon）

用 `switcherIcon` 替代自定义的展开箭头和 CSS 动画：

```javascript
const renderSwitcherIcon = ({ expanded }) => (
    <span className={style.treeExpandIcon}>
        <RightOutlined style={{
            transform: expanded ? 'rotate(90deg)' : 'rotate(0deg)',
            opacity: expanded ? 0.95 : 0.72,
            transition: 'all 0.22s cubic-bezier(0.4, 0, 0.2, 1)'
        }} />
    </span>
)
```

使用 CSS `transition` 替代原有的 `@keyframes` 动画，效果一致但代码更简洁。

### 4.5 JSX 替换

```jsx
<!-- 之前（约第 434-453 行） -->
<Menu
    mode="inline"
    inlineIndent={18}
    openKeys={openKeys}
    selectedKeys={selectedKeys}
    onOpenChange={handleOpenChange}
    expandIcon={() => null}
    items={transformToMenuItems(folderTree)}
    onClick={(e) => { /* 导航逻辑 */ }}
/>

<!-- 之后 -->
<Tree
    showIcon
    blockNode
    showLine={{ showLeafIcon: false }}
    treeData={transformToTreeData(folderTree)}
    expandedKeys={openKeys}
    selectedKeys={selectedKeys}
    onExpand={handleOpenChange}
    onSelect={(keys, info) => {
        if (info.node) handleTreeNodeClick(info.node)
    }}
    titleRender={renderTitle}
    switcherIcon={renderSwitcherIcon}
    draggable
    allowDrop={handleAllowDrop}
    onDrop={handleDrop}
/>
```

### 4.6 点击导航

```javascript
const handleTreeNodeClick = (node) => {
    const key = node.key
    if (key.slice(0, 4) === 'file') {
        preview(key.slice(4))
    } else if (key.startsWith('/content/') || key.startsWith('/excel/')) {
        window.open(`${window.location.origin}${window.location.pathname}#${key}`, '_blank')
    } else {
        navigate(key)
    }
}
```

逻辑与原 Menu `onClick` 一致，只是从 `e.key` 改为 `node.key`。

## 五、拖拽排序逻辑

### 5.1 限制同层拖拽（allowDrop）

```javascript
const handleAllowDrop = ({ dragNode, dropNode, dropPosition }) => {
    // dropPosition === 0 表示放入节点内部（作为子节点），禁止
    if (dropPosition === 0) return false

    // 通过 pos 属性判断是否同层
    // pos 格式如 "0-2-1"，去掉最后一段即为父路径
    // 同父路径 = 同层兄弟节点
    const dragParentPos = dragNode.pos.split('-').slice(0, -1).join('-')
    const dropParentPos = dropNode.pos.split('-').slice(0, -1).join('-')

    return dragParentPos === dropParentPos
}
```

### 5.2 处理拖放（onDrop）

```javascript
import { sortTreeItems } from '@/apis/fileList'

const handleDrop = async (info) => {
    const dragNode = info.dragNode
    const dropNode = info.node

    // 二次校验同层
    const dragParentPos = dragNode.pos.split('-').slice(0, -1).join('-')
    const dropParentPos = dropNode.pos.split('-').slice(0, -1).join('-')
    if (dragParentPos !== dropParentPos) return

    // 1. 乐观更新：立即修改本地树数据
    const newTreeData = reorderTreeData(folderTree, dragNode, dropNode)
    SetFolderTree(newTreeData)

    // 2. 构建有序 ID 列表
    const parentFolderId = findParentFolderId(folderTree, dropParentPos)
    const siblings = findSiblingsByPos(newTreeData, dropParentPos)
    const orderedIds = siblings.map(item => ({ id: item.id, status: item.status }))

    // 3. 持久化
    try {
        await sortTreeItems(parentFolderId, orderedIds)
    } catch (e) {
        error({ content: '排序保存失败，请重试' })
        getTree()
    }
}
```

**乐观更新策略：**
- 拖拽完成后立即更新 UI（用户无感知延迟）
- 后台调用排序接口
- 接口失败时回滚（重新获取树数据）并提示用户

### 5.3 辅助函数

```javascript
// 重新排序树数据（不可变操作，返回新数组）
const reorderTreeData = (treeData, dragNode, dropNode) => {
    const newTreeData = JSON.parse(JSON.stringify(treeData))

    const dragPath = dragNode.pos.split('-').map(Number)
    const dropPath = dropNode.pos.split('-').map(Number)
    const dragIndex = dragPath.pop()
    const dropIndex = dropPath.pop()

    // 沿路径找到父级数组
    const getArrayByPath = (data, path) => {
        let current = data
        for (const idx of path) {
            current = current[idx].children
        }
        return current
    }

    const array = getArrayByPath(newTreeData, dragPath)

    // 从原位置移除
    const [removed] = array.splice(dragIndex, 1)

    // 计算插入位置（同层数组中移除元素后索引需调整）
    let insertIndex = dropIndex
    if (dragPath.join('-') === dropPath.join('-') && dragIndex < dropIndex) {
        insertIndex--
    }

    // 插入到新位置
    array.splice(insertIndex, 0, removed)
    return newTreeData
}

// 根据 pos 字符串找到同级兄弟节点数组
const findSiblingsByPos = (treeData, posString) => {
    if (!posString) return treeData
    const path = posString.split('-').map(Number)
    let current = treeData
    for (const idx of path) {
        current = current[idx]?.children || []
    }
    return current
}

// 根据 pos 字符串找到父文件夹 ID（用于传给后端接口）
const findParentFolderId = (treeData, posString) => {
    if (!posString) return null  // 根目录
    const path = posString.split('-').map(Number)
    let current = treeData
    for (let i = 0; i < path.length - 1; i++) {
        current = current[path[i]].children
    }
    return current[path[path.length - 1]]?.rawData?.id || null
}
```

## 六、样式迁移

### 6.1 需要移除的样式

`src/views/Home/index.module.less` 中以下内容需删除：

**Menu 选择器（`<style>` 中的 `:global` 块）：**
- `.ant-menu-inline .ant-menu-item` — 节点间距
- `.ant-menu-inline .ant-menu-submenu-title` — 子菜单标题间距
- `.ant-menu-root > .ant-menu-submenu > .ant-menu-sub.ant-menu-inline` — 一级引导线
- 二级、三级嵌套引导线选择器
- `.ant-menu-inline .ant-menu-item-selected` — 选中高亮

**展开动画相关类：**
- `.menuExpandIcon` — 箭头容器
- `.menuExpandIconOpenState` — 展开状态
- `.menuExpandIconCloseState` — 折叠状态
- `.menuExpandIconAnimateOpen` — 展开动画
- `.menuExpandIconAnimateClose` — 折叠动画
- `@keyframes menuExpandOpen` — 展开关键帧
- `@keyframes menuExpandClose` — 折叠关键帧
- `@media (prefers-reduced-motion: reduce)` 中的覆盖

### 6.2 需要新增的样式

```less
// 展开箭头（替代 .menuExpandIcon 系列）
.treeExpandIcon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 12px;
    height: 12px;

    svg {
        width: 10px;
        height: 10px;
    }
}

:global {
    // 节点间距
    .ant-tree-treenode {
        padding: 1px 0;
    }

    // 选中高亮（与原 Menu 一致）
    .ant-tree-treenode-selected .ant-tree-node-content-wrapper {
        background-color: rgba(22, 119, 255, 0.08) !important;
        font-weight: 500;
    }

    // 紧凑行高
    .ant-tree .ant-tree-node-content-wrapper {
        height: 28px;
        line-height: 28px;
    }

    // 拖拽视觉反馈
    .ant-tree-treenode-draggable {
        cursor: grab;
    }

    .ant-tree-treenode-drag-over {
        background-color: rgba(22, 119, 255, 0.06);
    }

    // 缩进单位（对应原 inlineIndent={18}）
    .ant-tree-indent-unit {
        width: 18px;
    }
}
```

## 七、可移除的代码

重构到 Tree 后，以下代码可安全删除：

| 代码 | 原位置（约） | 原因 |
|------|-------------|------|
| `animatingExpandKey` state | 第 82 行 | 改用 `switcherIcon` 内联 transition |
| `expandAnimationType` state | 第 83 行 | 同上 |
| `expandAnimationTimerRef` ref | 第 84 行 | 同上 |
| `playExpandAnimation` 函数 | 第 198-208 行 | 同上 |
| 展开/折叠 cleanup effect | 第 324-330 行 | 同上 |
| `transformToMenuItems` 函数 | 第 232-280 行 | 被 `transformToTreeData` 替代 |

## 八、实施步骤

按顺序执行，每步完成后应用应可正常运行：

```
第 1 步：添加 sortTreeItems API 函数（src/apis/fileList.js）
    ↓
第 2 步：替换 transformToMenuItems 为 transformToTreeData
    ↓
第 3 步：添加 renderTitle、renderSwitcherIcon、handleTreeNodeClick
    ↓
第 4 步：将 JSX 中的 Menu 替换为 Tree（不启用拖拽，不加 draggable/allowDrop/onDrop）
    ↓
第 5 步：迁移 CSS 样式
    ↓
第 6 步：确认外观和导航功能与原来一致（测试验证）
    ↓
第 7 步：添加拖拽逻辑（draggable + allowDrop + onDrop + 辅助函数）
    ↓
第 8 步：删除不再使用的旧代码
    ↓
第 9 步：完整测试
```

## 九、测试验证清单

### 视觉回归

- [ ] 展开/折叠动画平滑
- [ ] 树形引导线正常显示
- [ ] 各类型图标正确（文档/文件夹/Excel/文件）
- [ ] Tooltip 正常显示
- [ ] 选中项高亮样式正确
- [ ] 侧边栏滚动条样式正确
- [ ] 侧边栏宽度拖拽调整正常

### 导航功能

- [ ] 点击文件夹 → 跳转到 `/home/list/:id`
- [ ] 点击文档 → 新标签页打开 `/content/:folder/:id`
- [ ] 点击 Excel → 新标签页打开 `/excel/:folder/:id`
- [ ] 点击文件 → 触发预览
- [ ] 面包屑同步更新
- [ ] URL 直接访问深层页面 → 父文件夹自动展开

### 拖拽排序

- [ ] 同层级拖拽排序正常
- [ ] 跨层级拖拽被阻止（不出现放置指示器）
- [ ] 拖入节点内部被阻止
- [ ] 拖拽后刷新页面，顺序保持
- [ ] 网络失败时自动回滚 + 显示错误提示

### 边界情况

- [ ] 只有 1 个子项时拖拽不崩溃
- [ ] 3 层以上嵌套正常工作
- [ ] 快速连续拖拽无异常
- [ ] 侧边栏折叠状态下正常显示
