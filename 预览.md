# 上传文件预览 API（前端对接）

## 1. 适用范围

本文档仅描述文件树中 `status=4` 的普通上传文件预览。

- `status=1`：在线文本，由系统内置文本编辑器处理，不走本接口。
- `status=2`：文件夹，不支持预览。
- `status=3`：在线 Excel，由系统内置表格编辑器处理，不走本接口。
- `status=4`：普通上传文件，使用本文档中的预览接口。

前端不要使用文件节点的 MinIO `url` 直接预览，应使用文件节点的 `id`
访问后端预览接口，以保证 JWT 鉴权、文件夹权限校验和 HTTP Range 正常生效。

## 2. 基础信息

### 2.1 API Base URL

服务器通过 Nginx 访问：

```text
http://101.43.146.27/new-app/api
```

同源部署时，前端建议使用相对地址：

```ts
const API_BASE = "/new-app/api";
```

后续文档中的接口路径均相对于 `API_BASE`。

### 2.2 认证

除登录、验证码等公开接口外，本文档中的接口都需要 JWT：

```http
Authorization: Bearer <token>
```

Token 缺失、格式错误或过期时返回 HTTP `401`，响应体可能为空。

### 2.3 JSON 统一响应

元信息、资源树和 Office 任务接口使用统一结构：

```ts
interface Result<T> {
  code: number;
  message: string;
  data: T | null;
}
```

成功示例：

```json
{
  "code": 200,
  "message": "",
  "data": {}
}
```

前端必须同时判断 HTTP 状态和响应体中的 `code`。特别是预览元信息接口，
参数或权限错误可能返回 HTTP `200`，但响应体 `code` 为 `400` 或 `403`。

## 3. 接口总览

| 用途 | 方法 | 路径 |
| --- | --- | --- |
| 获取预览元信息 | GET | `/document/preview/4/{fileId}/info` |
| 获取普通文件流 | GET | `/document/preview/4/{fileId}` |
| 提交 Office 转 PDF 任务 | POST | `/document/preview/4/{fileId}/prepare` |
| 获取转换后的 PDF 文件流 | GET | `/document/preview/4/{fileId}/pdf` |

## 4. 获取可访问上传文件树

```http
GET /excel/files
Authorization: Bearer <token>
```

该接口只返回当前用户有查看权限的目录和上传文件。

### 4.1 响应类型

```ts
interface AuthorizedFileTreeNode {
  id: number;
  status: 2 | 4;
  name: string;
  url: string | null;
  children: AuthorizedFileTreeNode[];
}
```

- `status=2`：文件夹节点。
- `status=4`：上传文件节点，可以将其 `id` 作为 `fileId`。
- 文件夹的 `url` 为 `null`。
- 前端预览时不要直接使用文件节点的 `url`。

### 4.2 响应示例

```json
{
  "code": 200,
  "message": "",
  "data": [
    {
      "id": 10,
      "status": 2,
      "name": "项目资料",
      "url": null,
      "children": [
        {
          "id": 430,
          "status": 4,
          "name": "10-26基础题.docx",
          "url": "http://minio.example/bucket/object",
          "children": []
        }
      ]
    }
  ]
}
```

### 4.3 服务端搜索

```http
GET /excel/files/search?keyword=基础题
Authorization: Bearer <token>
```

`keyword` 可以不传。返回结构与 `/excel/files` 相同。

## 5. 获取预览元信息

```http
GET /document/preview/4/{fileId}/info
Authorization: Bearer <token>
```

前端应先调用该接口，再决定使用普通文件流还是 Office PDF 流。

### 5.1 响应类型

```ts
type PreviewType = "file" | "office_pdf";

type OfficePreviewStatus =
  | "NOT_CREATED"
  | "QUEUED"
  | "CONVERTING"
  | "READY"
  | "FAILED"
  | "UNSUPPORTED";

interface PreviewInfo {
  fileName: string;
  contentType: string;
  fileSize: number | null;
  previewType: PreviewType;
  previewStatus: OfficePreviewStatus | null;
  previewError: string | null;
  pdfSize: number | null;
  content: null;
  author: null;
  workbook: null;
}
```

对于 `status=4`，`content`、`author`、`workbook` 不使用。

### 5.2 普通文件示例

```json
{
  "code": 200,
  "message": "",
  "data": {
    "fileName": "研究报告.pdf",
    "contentType": "application/pdf",
    "fileSize": 83051,
    "previewType": "file",
    "previewStatus": null,
    "previewError": null,
    "pdfSize": null,
    "content": null,
    "author": null,
    "workbook": null
  }
}
```

### 5.3 Office 文件示例

```json
{
  "code": 200,
  "message": "",
  "data": {
    "fileName": "10-26基础题.docx",
    "contentType": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "fileSize": 19251,
    "previewType": "office_pdf",
    "previewStatus": "READY",
    "previewError": null,
    "pdfSize": 83051,
    "content": null,
    "author": null,
    "workbook": null
  }
}
```

### 5.4 前端分支

```ts
if (info.previewType === "file") {
  // 使用普通文件流接口。
}

if (info.previewType === "office_pdf") {
  // 根据 previewStatus 决定提交任务、继续轮询或加载 PDF。
}
```

## 6. 获取普通文件流

```http
GET /document/preview/4/{fileId}
Authorization: Bearer <token>
Range: bytes=0-1023
```

这是二进制流接口，不使用 `Result<T>` 包装。

### 6.1 全量响应

未发送 `Range` 时：

```http
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Disposition: inline; filename="..."
Content-Length: 83051
Accept-Ranges: bytes
```

### 6.2 Range 响应

发送合法 `Range` 时：

```http
HTTP/1.1 206 Partial Content
Content-Type: application/pdf
Content-Range: bytes 0-1023/83051
Content-Length: 1024
Accept-Ranges: bytes
```

支持：

```text
bytes=0-1023
bytes=1024-
bytes=-1024
```

范围无法满足时返回 HTTP `416`：

```http
Content-Range: bytes */83051
```

当前不支持多段 Range，例如：

```text
bytes=0-99,200-299
```

### 6.3 前端加载示例

```ts
async function fetchPreviewBlob(fileId: number, token: string): Promise<Blob> {
  const response = await fetch(
    `${API_BASE}/document/preview/4/${fileId}`,
    {
      headers: {
        Authorization: `Bearer ${token}`
      }
    }
  );

  if (!response.ok) {
    throw new Error(`文件预览失败：HTTP ${response.status}`);
  }

  return response.blob();
}
```

使用完成后必须释放 Blob URL：

```ts
const blob = await fetchPreviewBlob(fileId, token);
const objectUrl = URL.createObjectURL(blob);

// 将 objectUrl 赋给 img、iframe、video 或 audio。

URL.revokeObjectURL(objectUrl);
```

PDF iframe 不要设置空 `sandbox`，否则 Chrome 会屏蔽内置 PDF 查看器：

```ts
const iframe = document.createElement("iframe");
iframe.src = objectUrl;
iframe.title = info.fileName;
// 不要设置 iframe.setAttribute("sandbox", "");
```

## 7. Office 转 PDF

支持的源文件扩展名：

```text
doc, docx, rtf, odt
xls, xlsx, ods
ppt, pptx, odp
```

Office 文件不会直接交给浏览器渲染。后端通过 LibreOffice 转成 PDF，前端再加载
PDF 文件流。

### 7.1 提交转换任务

```http
POST /document/preview/4/{fileId}/prepare
Authorization: Bearer <token>
```

无请求体。该接口幂等，重复调用不会重复创建相同任务。

响应示例：

```json
{
  "code": 200,
  "message": "",
  "data": {
    "fileName": "10-26基础题.docx",
    "contentType": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "fileSize": 19251,
    "previewType": "office_pdf",
    "previewStatus": "QUEUED",
    "previewError": null,
    "pdfSize": null,
    "content": null,
    "author": null,
    "workbook": null
  }
}
```

### 7.2 状态处理

| 状态 | 前端处理 |
| --- | --- |
| `NOT_CREATED` | 调用 `/prepare` |
| `QUEUED` | 显示“排队中”，继续轮询 `/info` |
| `CONVERTING` | 显示“转换中”，继续轮询 `/info` |
| `READY` | 加载 `/pdf` |
| `FAILED` | 显示 `previewError`，提供重试入口 |
| `UNSUPPORTED` | 显示 `previewError`，改为下载源文件 |

建议每 `1500~2000ms` 请求一次 `/info`，总等待时间由前端控制，例如两分钟。
页面销毁、切换文件或用户取消时必须停止轮询。

### 7.3 获取转换后的 PDF

```http
GET /document/preview/4/{fileId}/pdf
Authorization: Bearer <token>
Range: bytes=0-1023
```

该接口也是二进制流，支持与普通文件接口相同的 HTTP Range。

当 PDF 尚未就绪时返回：

```http
HTTP/1.1 409 Conflict
Content-Type: application/json
```

```json
{
  "code": 409,
  "message": "Office PDF 预览尚未生成完成",
  "data": null
}
```

## 8. 推荐的完整调用流程

```text
GET /excel/files
    ↓ 选择 status=4 节点并取得 fileId
GET /document/preview/4/{fileId}/info
    ├─ previewType=file
    │      ↓
    │  GET /document/preview/4/{fileId}
    │
    └─ previewType=office_pdf
           ├─ READY → GET /document/preview/4/{fileId}/pdf
           ├─ NOT_CREATED → POST /prepare
           ├─ QUEUED / CONVERTING → 轮询 /info
           └─ FAILED / UNSUPPORTED → 显示原因并提供源文件下载
```

## 9. 文件类型与前端渲染建议

| 类型 | 扩展名示例 | 建议 |
| --- | --- | --- |
| 图片 | jpg、png、gif、webp、tiff | `<img>` |
| PDF | pdf | 无 `sandbox` 的 `<iframe>` 或 `<embed>` |
| 视频 | mp4、webm、mov 等 | `<video controls>`，能否播放取决于浏览器编码 |
| 音频 | mp3、wav、ogg、flac、aac | `<audio controls>` |
| 文本 | txt、md、csv、css、json | 读取 Blob 文本后放入 `<pre>` |
| Office | docx、xlsx、pptx 等 | 转换完成后按 PDF 预览 |
| 压缩包 | zip、rar、7z、tar、gz | 不预览，提供下载 |
| 未知类型 | 其他扩展名 | 不预览，提供下载 |

上传的 `.md` 是 `status=4` 文件时，当前只显示 Markdown 原文，不进行 Markdown
编译。在线文本编辑器中的 Markdown/ProseMirror 内容属于 `status=1`，不在本文档范围。

出于安全考虑，以下主动内容不应放入同源 iframe：

```text
html, htm, svg, xml, js
```

前端应提供下载按钮，不要使用 `innerHTML` 渲染其内容。

## 10. 错误处理

| HTTP/业务码 | 场景 | 前端处理 |
| --- | --- | --- |
| `200` | 成功；或 `/info` 返回业务错误 | JSON 接口继续检查响应体 `code` |
| `400` | 参数错误、文件不存在、类型不支持 | 显示 `message` |
| `401` | Token 缺失或过期 | 清理登录状态并跳转登录 |
| `403` | 没有文件所在目录的 VIEW 权限 | 提示无查看权限 |
| `409` | Office PDF 尚未就绪 | 继续轮询 `/info` |
| `416` | Range 超出文件大小 | 重新获取元信息并重置 Range |
| `429` | Office 转换队列已满 | 稍后重试 |
| `500` | 转换、MinIO 或服务内部错误 | 显示错误并允许重试 |

JSON 错误响应通常为：

```json
{
  "code": 403,
  "message": "无预览该资源的权限",
  "data": null
}
```

流式接口发生异常时，响应可能是 JSON 错误，也可能是在传输过程中断开。前端除检查
HTTP 状态外，还应捕获网络错误和 Blob 长度异常。

## 11. 前端请求封装参考

```ts
const API_BASE = "/new-app/api";

class ApiError extends Error {
  constructor(
    message: string,
    public readonly httpStatus: number,
    public readonly code?: number
  ) {
    super(message);
  }
}

async function requestJson<T>(
  path: string,
  token: string,
  init: RequestInit = {}
): Promise<T> {
  const response = await fetch(`${API_BASE}${path}`, {
    ...init,
    headers: {
      ...(init.body ? { "Content-Type": "application/json" } : {}),
      ...init.headers,
      Authorization: `Bearer ${token}`
    }
  });

  if (response.status === 401) {
    throw new ApiError("登录已过期", 401, 401);
  }

  const result = (await response.json()) as Result<T>;
  if (!response.ok || result.code !== 200 || result.data === null) {
    throw new ApiError(
      result.message || `请求失败：HTTP ${response.status}`,
      response.status,
      result.code
    );
  }

  return result.data;
}

export function getAuthorizedFiles(token: string) {
  return requestJson<AuthorizedFileTreeNode[]>("/excel/files", token);
}

export function getPreviewInfo(fileId: number, token: string) {
  return requestJson<PreviewInfo>(
    `/document/preview/4/${fileId}/info`,
    token
  );
}

export function prepareOfficePreview(fileId: number, token: string) {
  return requestJson<PreviewInfo>(
    `/document/preview/4/${fileId}/prepare`,
    token,
    { method: "POST" }
  );
}
```

## 12. Nginx 注意事项

`/new-app/api/` 需要转发 `Range` 和 `If-Range`：

```nginx
proxy_set_header Range $http_range;
proxy_set_header If-Range $http_if_range;
```

建议暴露以下响应头：

```nginx
add_header Access-Control-Expose-Headers
    "Content-Disposition, Content-Length, Content-Range, Accept-Ranges"
    always;
```

前端与 API 同源时不依赖 CORS 暴露头；跨域部署时，如果前端需要读取
`Content-Range` 等响应头，则必须配置上述 `Access-Control-Expose-Headers`。
